import { setCardCount } from '../../../helpers/set-card-count';
import { module, test } from 'qunit';

module('Unit | Helper | set card count');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = setCardCount(42);
  assert.ok(result);
});
