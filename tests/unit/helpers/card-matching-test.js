import { cardMatching } from '../../../helpers/card-matching';
import { module, test } from 'qunit';

module('Unit | Helper | card matching');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = cardMatching(42);
  assert.ok(result);
});
