<div class="container">
  <div class="row">
    <div class="col-sm-3">
      {{#each model.cards as |card|}}
        {{edit-collection-card session=session card=card join=model.join addCard="addCard"}}
      {{/each}}
    </div>
    <div class="col-sm-9">
      {{my-collection-display session=session join=model.join}}
    </div>
  </div>
</div>
